import React from 'react';
import { Button } from 'antd';
import './App.css';

const App = () => (
  <div className="App">
    <p> Start Building React apps with Antdesign </p>
    <Button type="ghost">Start Coding</Button>
    <Button type="primary">Now</Button>
  </div>
);

export default App;
